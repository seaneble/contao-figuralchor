{% extends '@Contao/block_unsearchable.html.twig' %}

{% block content %}
    {% if not articles %}
        <p class="empty">{{ empty }}</p>
    {% else %}
        {% for article in articles %}{{ article.html|raw }}{% endfor %}
    {% endif %}
{% endblock %}
