{% extends '@Contao/block_searchable.html.twig' %}

{% block content %}
    {{ articles|raw }}
    {% if referer|default %}
        <!-- indexer::stop -->
        <p class="back"><a href="{{ referer }}" title="{{ back }}">{{ back }}</a></p>
        <!-- indexer::continue -->
    {% endif %}
{% endblock %}
